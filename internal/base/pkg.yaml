name: base
variant: scratch
shell: /toolchain/bin/bash
dependencies:
  - image: "ghcr.io/siderolabs/base:{{ .BUILD_ARG_PKGS_UPSTREAM }}"
  - image: "ghcr.io/siderolabs/ca-certificates:{{ .BUILD_ARG_PKGS_UPSTREAM }}"
finalize:
  - from: /
    to: /
